# TODO

- [ ] Проверить, нужны ли провайдеры `null` и `local` — они были declared в `versions.tf`, но нигде не использовались и были удалены
- [x] Убрать default значения из helm-values.yaml, которые совпадают с дефолтами контейнера Nora
- [ ] ⚠️ Аутентификация ОТКЛЮЧЕНА — все эндпоинты публично доступны без авторизации. Для продакшена нужно включить auth (`NORA_AUTH_ENABLED=true`)
- [ ] ⚠️ Аутентификация ОТКЛЮЧЕНА — все эндпоинты публично доступны без авторизации. Для продакшена нужно включить auth (`NORA_AUTH_ENABLED=true`)
